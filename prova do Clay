
package main

import "fmt"

func main() {
	var id,age,cpf int
	var name,location string
	var income float
	var PERSONAL,GUARANTEED,CONSIGNMENT bool = false,false.false
	fmt.println("digite seu nome , idade, cpf , salario e localidade") 
	fmt.scanln
	
	
	if(income<3000){
	PERSONA=GUARENTEED=TRUE
	}	
	else if(income<5000){
		if(age<30 && location==SP)
		PERSONA=GUARENTEED=TRUE
		}
	else
	CONSIGNMENT=TRUE
	}
	
	fmt.println("%s você tem acesso  "name)
	If(CONSIGNMENT==true) {
           fmt.println("ao empréstimo consignado com 2% de juros/n")}
        If (PERSONA==true ) {
           fmt.println("ao empréstimo pessoal com 4% de juros/n")
        If (GUARANTEED==true ){
           fmt.println("ao empréstimo com garantia de 3% de juros/n")
        If(GUARANTEED==false && PERSONA==false && CONSIGNMENT==false) {
           fmt.println("a nenhum empréstimo")
}

